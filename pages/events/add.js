import Layout from '@/components/Layout';

const AddEventPage = () => {
    return (
        <Layout title='Add New Event'>
            <div>
                <h1>Add Event</h1>
            </div>
        </Layout>
    );
};

export default AddEventPage;
